# @package _global_

ideator_args:
  _target_: core.ideators.base.Ideator
  secrets: ${secrets}
  model_url: ${model_url}
  model_name: ${model_name}
  system_prompt: ${system_prompt}
  log_llm_metrics: ${log_llm_metrics}